# === SECRETS & KEYS ===
.env
.env.*
!.env.example
secrets.json
secrets.toml
config.secret.*
api_keys.*
virustotal.key
*.pem
*.key
*.p12
*.pfx
*.cer

# === BUILD ARTIFACTS ===
dist/
build/
target/
src-tauri/target/

# === NODE ===
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# === LOGS ===
.logs/
*.log
logs/

# === OS ===
Thumbs.db
Desktop.ini
.DS_Store
desktop.ini
ehthumbs.db

# === IDE ===
.vscode/
.idea/
*.suo
*.user
*.userprefs
*.vsix

# === CLAMAV DB (téléchargés à runtime, non committés) ===
clamav-*/
*.cvd
*.cld

# === PORTABLE ZIP / RELEASE ===
*.zip
*.7z

# Dossier de preparation du canal de mise a jour (outils/publier.py)
release/

# Signature de code : empreinte locale, gitignoree pour ne pas casser le build
# de qui ne possede pas le certificat (CI comprise).
# Voir D:/Projet/outils/signer/LISEZMOI.md
src-tauri/tauri.signing.conf.json

# Signature de code : empreinte locale + installateur de certificat.
# Gitignore pour ne pas casser le build de qui ne possede pas le
# certificat (CI comprise). Voir D:/Projet/outils/signer/LISEZMOI.md
src-tauri/certificat/
